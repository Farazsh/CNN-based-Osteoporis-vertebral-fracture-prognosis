import torch
import os
import wandb
from src.custom_logging import WANDB_TEAM_LOGIN_KEY

print(torch.cuda.is_available())
# wandb.login(key=WANDB_TEAM_LOGIN_KEY)
# wandb.init()
# wandb.finish()
input()
